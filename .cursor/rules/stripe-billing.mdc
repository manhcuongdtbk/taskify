---
description: Billing / Stripe / monetization work — read docs/billing.md first
globs: "{docs/billing.md,**/stripe*,**/billing/**,lib/subscription.ts,actions/stripe-redirect/**,app/api/webhook/**,constants/pricing-plans.ts,prisma/schema.prisma}"
alwaysApply: false
---

# Billing in this repo

Before proposing or implementing billing, Stripe, or monetization changes: read [`docs/billing.md`](docs/billing.md). That file owns concepts, backlog priority (harden before growth), and diagrams.

Do not introduce a parallel billing stack unless the user explicitly asks.

Use `autonumber` on new `sequenceDiagram`s (see mermaid-autonumber rule).
